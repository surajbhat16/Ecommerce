# Normalize line endings so bash scripts run on Linux CI runners even when
# committed from Windows (CRLF in a shebang line breaks /usr/bin/env bash).
* text=auto
*.sh text eol=lf
Makefile text eol=lf
*.ps1 text eol=crlf
